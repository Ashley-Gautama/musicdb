<p id="notice"><%= notice %></p>

<h1>Listing Artists</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      

      <th colspan="3"></th>
    </tr>
  </thead>
  <%if user_signed_in?%>
  <%if current_user.email=='frank@ex.com'%>
  <% end %>
  <%= link_to 'New Artist', new_artist_path %>


  <% end %>
<br>
<br>
<br>

  <tbody>
    <% @artists.each do |artist| %>
      <tr>
        <td><%=link_to artist.name, artist_path(artist) %>
</td>


<%if user_signed_in?%>
<%if current_user.email=='frank@ex.com'%>

        <td><%= link_to 'Edit', edit_artist_path(artist) %>
</td>

        <td><%= link_to 'Destroy', artist_path(artist), method: :delete, data: { confirm: 'Are you sure?' } %>
</td>


        <% end %>


        <% end %>



      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Back', root_path %>
