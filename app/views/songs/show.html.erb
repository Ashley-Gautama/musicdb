<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @song.title %>
</p>

<p>
  <strong>Album:</strong>
  <%= @song.album %>
</p>

<p>
  <strong>Description:</strong>
  <%= @song.description %>
</p>

<p>
  <strong>Cover:</strong>
<img class="big-cover" src="<%= @song.cover %>"/></p>

<%if user_signed_in?%>
<%if current_user.email=='frank@ex.com'%>
<%= link_to 'Edit', edit_artist_song_path(@song.artist_id, @song.id)%>
<%= link_to 'New Song', new_artist_song_path(artist_id: @song.artist.id)%>
<td><%= link_to 'Destroy', artist_song_path(@song.artist_id, @song.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>

        <% end %>
        <% end %>



<%= link_to 'Back', songs_path %>
